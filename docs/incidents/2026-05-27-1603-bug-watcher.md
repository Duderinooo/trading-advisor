## ISSUE: JSON schema strictTypes validation error

**Severity:** critical  
**Pattern:** `"strict mode: use allowUnionTypes to allow union type keyword at".*new_take_profit".*strictTypes`  
**Frequency:** 3 occurrences in window  
**First seen:** 2026-05-27T09:10:25  
**Last seen:** 2026-05-27T15:35:23

**Excerpt:**
```
agents._lib.runner.AgentRunError: claude CLI exit=1: strict mode: use allowUnionTypes to allow union type keyword at "#/properties/actions/items/oneOf/2/properties/input/properties/new_take_profit" (strictTypes)
```

**Hypothesis:** Tool schema for `update_position` or `recommend_entry` uses union type (`oneOf`) on `new_take_profit` field. Schema not marked with `allowUnionTypes` in claude CLI config.

**Suggested action:** Find tool definition with `new_take_profit` union. Add `"allowUnionTypes": true` to schema or flatten union. Test with `call_claude_agent(mode="event")` to verify.

---

✅ All other systems nominal (no silence, no new exception types, news/price monitoring running).